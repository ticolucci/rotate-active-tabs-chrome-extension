/**
 * Sample test file for the Rotate Active Tabs extension
 * This demonstrates the test infrastructure setup
 */

describe('Extension Boilerplate', () => {
  it('should have a valid test environment', () => {
    expect(true).toBe(true);
  });

  it('should have access to document object', () => {
    expect(document).toBeDefined();
  });
});
